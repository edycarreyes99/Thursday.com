import { TaskCategoryModel } from './task-category-model';

describe('TaskCategoryModel', () => {
  it('should create an instance', () => {
    expect(new TaskCategoryModel()).toBeTruthy();
  });
});
